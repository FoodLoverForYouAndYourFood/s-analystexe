# Матчер вакансий - Chrome Extension

Расширение для проверки вакансий через локальный API сервер с GigaChat.

## Установка

### 1. Запусти сервер

```bash
cd ../server
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Создай .env и добавь ключ GigaChat
cp .env.example .env
nano .env  # Вставь GIGACHAT_AUTH_KEY=твой_ключ

# Запусти сервер
python app.py
```

Сервер должен запуститься на `http://localhost:5000`

**Получить ключ GigaChat:**
1. Зарегистрируйся на [developers.sber.ru/studio](https://developers.sber.ru/studio/)
2. Создай проект → GigaChat API
3. Настройки API → Получить ключ
4. Скопируй **Authorization Key**

### 2. Установи расширение в Chrome

1. Открой Chrome → `chrome://extensions/`
2. Включи **«Режим разработчика»** (справа вверху)
3. Нажми **«Загрузить распакованное расширение»**
4. Выбери папку `extension-chrome`
5. Готово! Иконка появится в панели расширений

## Использование

### Первый запуск

1. Убедись что сервер запущен на localhost:5000
2. Открой расширение (кликни на иконку)
3. Перейди в таб **👤 Профиль**
4. Вставь `MATCHER_API_KEY` (из `.env` сервера)
5. Скопируй текст резюме и вставь в поле (можно из PDF, DOCX или с сайта)
6. Укажи зарплату, формат работы, red flags
7. Сохрани профиль

### Проверка вакансии

1. Открой вакансию на hh.ru, LinkedIn или Habr
2. Кликни на иконку расширения
3. Если нужен полный веб‑матчер, нажми **«Открыть матчер в браузере»**
4. Нажми **«📋 Взять со страницы»** или вставь текст вручную
5. Нажми **«Проверить совпадение»**
6. Получи детальный анализ:
   - Информация о компании (размер, репутация, отрасль)
   - Совпадение с требованиями
   - Карьерные перспективы
   - Рыночный анализ (зарплата vs рынок)
   - Риски и возможности
   - Персональные рекомендации

## Архитектура

```
Расширение (popup.js)
    ↓ HTTP POST /api/analyze
Локальный сервер (Flask)
    ↓ GigaChat API call
GigaChat AI
```

**Изменения vs старая версия:**
- ✅ Убран таб "Настройки" - ключ API теперь на сервере
- ✅ Упрощенный код - только вызов локального API
- ✅ Расширенный промпт - анализ компании, рынка, карьеры
- ✅ Более детальные рекомендации

## Поддерживаемые сайты

- hh.ru
- LinkedIn
- Habr Career
- career.habr.com

## Поддерживаемые форматы резюме

Любые - просто скопируй текст и вставь:
- **PDF** — открой PDF, выдели весь текст (Ctrl+A), скопируй (Ctrl+C), вставь в расширение
- **DOCX** — открой в Word/LibreOffice, скопируй текст, вставь
- **hh.ru / LinkedIn** — открой своё резюме на сайте, скопируй текст со страницы

## Требования

- Chrome 88+
- Python 3.8+ (для сервера)
- GigaChat API key
- Локальный сервер должен быть запущен

## Troubleshooting

**Ошибка "Server error":**
- Проверь что сервер запущен: `curl http://localhost:5000/health`
- Проверь что ключ GigaChat в .env файле
- Посмотри логи сервера

**Не работает "Взять со страницы":**
- Проверь что ты на поддерживаемом сайте (hh.ru, LinkedIn, Habr)
- Попробуй скопировать текст вручную

## Roadmap

- [ ] Firefox версия
- [ ] Поддержка PDF/DOCX напрямую (пока копируй текст вручную)
- [x] Боковая панель (не закрывается при переходах)
- [ ] История проверенных вакансий
- [ ] Поддержка других LLM (OpenAI, Claude)

## Примечания

**Side Panel:** Расширение открывается как боковая панель (как DevTools). Она не закрывается при клике вне её и остаётся открытой при переходе между вкладками.
